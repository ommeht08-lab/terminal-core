# Copy this file to .env.local and fill in real values for local development.
# If NEXT_PUBLIC_API_URL is unset, the app falls back to http://127.0.0.1:8000
# automatically -- only needed to point at a deployed backend (Render, etc.).
# Must be set in Vercel's project env config for production.

# NEXT_PUBLIC_API_URL=https://terminal-core-api.onrender.com
